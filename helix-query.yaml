auto-generated: true
indices:
  blog:
    include:
      - /blog/**
    target: /blog/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      date:
        select: head > meta[name="publication-date"]
        value: |
          dateValue(attribute(el, 'content'), 'MM-DD-YYYY')
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
      featured:
        select: head > meta[name="featured"]
        value: |
          attribute(el, "content")
      author:
        select: head > meta[name="author"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      content:
        select: main > div
        values: characters(textContent(el), 0, 200)
      tags:
        select: head > meta[property="article:tag"]
        values: |
          attribute(el, 'content')
  authors:
    include:
      - /author/**
    target: /author/query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, "content")
  events:
    include:
      - /events-and-promotions/schedule-of-events/**
    target: /events-and-promotions/schedule-of-events/query-index.json
    properties:
      title:
        select: main h1
        value: |
          textContent(el)
      description:
        select: head > meta[name="description"]
        value: |
          attribute(el, "content")
      eventDate:
        select: head > meta[name="event-date"]
        value: |
          dateValue(attribute(el, "content"), "MM/DD/YYYY")
      eventTime:
        select: head > meta[name="event-time"]
        value: |
          attribute(el, "content")
      eventLocation:
        select: head > meta[name="event-location"]
        value: |
          attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      artistWebsite:
        select: head > meta[name="artist-website"]
        value: |
          attribute(el, "content")
      tickets:
        select: head > meta[name="tickets"]
        value: |
          attribute(el, "content")
      onsale:
        select: head > meta[name="onsale"]
        value: |
          dateValue(attribute(el, "content"), "MM/DD/YYYY")
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
